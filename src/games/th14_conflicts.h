/* No TH14 conflict sites are recorded yet: vpatch_th14.dll has not been read here, so rather
   than guess at addresses the game registers none and the module-name check in conflict.c
   covers it on its own. See ADDING_A_GAME.md for how these are found. */
static const struct ConflictSite* const th14_conflicts = NULL;
static const size_t th14_conflict_count = 0;
